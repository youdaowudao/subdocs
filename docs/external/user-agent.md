# User-Agent 说明

::: tip 待补充
这里先作为 User-Agent 相关说明的占位页。
:::

## 本页要说明什么

- 什么是 `User-Agent`
- 哪些客户端会自动带 `User-Agent`
- 需要排查限流、识别客户端或兼容问题时应该看哪些字段
- 是否需要用户手动配置

## 基础判断

普通用户优先不改 `User-Agent`。只有在后台要求识别来源、排查兼容性或定位异常请求时，再单独处理。

